<%= render_custom_relations_rows(issue) %>

<%# TODO: выводить скрытую форму для каждого поля. показывать по клику добавить/изменить %>

<%#= form_for CustomRelationsValue.new, {
                 :as => :custom_relations_value, :remote => true,
                 :url => custom_relations_path,
                 :method => :post,
                 :html => {:id => 'new-custom_relation-form', :style => 'display: none;'}
               } do |f| %>
<%#= render :partial => 'issues/custom_relations_form', :locals => {:f => f}%>
<%# end %>
